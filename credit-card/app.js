var creditCardNumber = prompt("Introduce your credit card number, please:");

function isValidCard () {
  var array = [];
  for(var i = 0 ; i < array.length ; i++) {
    var value = array.pop();
    array.splice(i, 0, value);
  }
}
isValidCard(array(creditCardNumber));
